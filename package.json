{
  "name": "snap",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ snap",
    "create-local": "graph create --node http://localhost:8020/ snap",
    "remove-local": "graph remove --node http://localhost:8020/ snap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 snap",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0",
    "@apollo/client": "^3.6.9",
    "@graphprotocol/client-cli": "^2.2.3",
    "apollo-cache-inmemory": "^1.6.6",
    "apollo-client": "^2.6.10",
    "apollo-link-http": "^1.5.17",
    "fs": "^0.0.1-security",
    "graphql": "^16.6.0",
    "node-fetch": "^3.2.8"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
